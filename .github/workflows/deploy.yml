name: Deploy to Lightsail

# Auto-deploy: on every push to the deploy branch, SSH into the Lightsail box,
# pull the latest code, and rebuild/restart the stack. Change the branch below
# to `main` once you merge and want main to be the deploy branch.
on:
  push:
    branches:
      - feat/live-navigation
  workflow_dispatch: {}   # lets you trigger a deploy manually from the Actions tab

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.LIGHTSAIL_HOST }}
          username: ${{ secrets.LIGHTSAIL_USER }}
          key: ${{ secrets.LIGHTSAIL_SSH_KEY }}
          command_timeout: 40m       # first build installs deps; later builds are cached
          script: |
            set -e
            cd ~/agentic-ecommerce
            git fetch --all
            git reset --hard origin/feat/live-navigation
            docker compose --env-file .env -f infra/docker/docker-compose.prod.yml up -d --build
            docker image prune -f
            docker compose --env-file .env -f infra/docker/docker-compose.prod.yml ps
