"""Request-scoped ContextVars.

Holds request_id, tenant_id, session_id for log enrichment and tenant-aware
queries. Currently a placeholder.

Populated in: Task 1.8 — Structured logging (request_id) and Module 2
(tenant_id).
"""
