"""SQLAlchemy async engine and session factory.

Configures connection pool, sets search path for RLS, exposes
AsyncSession dependency for FastAPI injection.

Populated in: Task 1.5 — Infrastructure layer skeleton.
"""
